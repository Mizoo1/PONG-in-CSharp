using Pong.Game.Models;

namespace Pong.Engine.Defaults
{
    /// <summary>
    /// A set of useful default positions for objects
    /// </summary>
    public static class Positions
    {
#pragma warning disable CS1591
        public static Position TopLeft = new Position { X = 20, Y = 20 };
        public static Position TopRight = new Position {  };
    }
}